# Technica Project Instructions

## 作業の進め方

- Issueへ着手するときは、現在のコードとIssueの要件を確認し、実装計画を提示すること。
- DB設計、リレーション、API契約、認証・認可、新しい依存パッケージ、ディレクトリ構成など、後続実装へ影響する重要な設計判断は実装前に相談すること。
- 軽微で可逆的な実装判断は、既存コードの規約に合わせて進めること。
- ユーザーが明示的に依頼しない限り、コミット、プッシュ、PR作成を行わないこと。
- 実装完了時に、変更内容に応じたコミット分割と日本語のコミットメッセージを提案すること。
- ユーザーが作成した変更や、依頼と無関係な変更を上書きしないこと。

## コードコメント

- 変更したファイルでは、コードを読めば分かる生成系・定型コメントを削除すること。
- メソッド名や型定義と同じ内容を繰り返すDocBlockは追加しないこと。
- 設計理由、制約、回避しにくい実装上の注意点など、コードだけでは判断できないコメントは残すこと。
- ライセンス表記や静的解析に必要な型情報は削除しないこと。

## Backend

- Laravelの責務に従い、入力検証はFormRequest、HTTP処理はController、APIレスポンス変換はResourceへ分離すること。
- DB設計では外部キー、削除時の挙動、インデックス、nullable、Eloquentリレーションを検討すること。
- API実装では正常系だけでなく、バリデーション、未認証、認可、存在しないリソースも検討すること。
- Backend変更後は`composer lint`と`composer test`を実行すること。

## Backendテスト

- PHPUnitのテストメソッド名は`test_{対象メソッド}_{条件}_{期待結果}`形式にすること。
- テスト名は、クラス名と組み合わせて対象機能が分かる名前にすること。
- ASCII部分はLaravel Pintに合わせて小文字のsnake_caseにすること。
- テスト名の例:
  - `test_store_入力値が有効な場合_ユーザーを登録できること`
  - `test_destroy_未認証の場合_認証エラーになること`
  - `test_show_ログイン中の場合_ログインユーザーを取得できること`

## Frontend

- Frontend変更後は`npm run lint`、`npm run typecheck`、`npm run test:run`、`npm run build`を実行すること。
- 既存のコンポーネント構成とデザイン規約を優先すること。

## Frontendテスト

- 単体・コンポーネントテストにはVitestとReact Testing Libraryを使用すること。
- `describe`には対象、`it`には`{条件}_{期待結果}`形式でテスト内容を書くこと。
- 実装詳細ではなく、ユーザーから見える表示や操作をテストすること。
- Snapshotテストは標準では使用しないこと。
- Async Server Componentは必要に応じてE2Eテストで確認すること。

## 完了報告

- 実装内容、設計判断、変更ファイル、検証結果を簡潔に報告すること。
- 未実施の確認や残っている課題があれば明記すること。
- コミットは作成せず、推奨するファイル分割と日本語コミットメッセージを提示すること。
